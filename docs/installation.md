# Installation

## Requirements

- Python **3.13** or newer

## Install via pip

```shell
pip install docstring-generator
```

## Install via uv

```shell
uv add docstring-generator
```

After installation the `gendocs_new` command is available on your `PATH`.

## Verify

```shell
gendocs_new --help
```
